import { createUseStyles } from "react-jss";

export default createUseStyles({
    Container: {
        display: 'inline-flex',
        flexDirection: "row",
        alignItems: 'center',
        marginLeft: 5
    },
    Pointer: {
        cursor: 'pointer'
    }
});